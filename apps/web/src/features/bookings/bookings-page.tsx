import { PageHeader } from "@/shared/components/page-header";

export function BookingsPage() {
  return (
    <PageHeader
      title="Bookings"
      description="Manage appointments for your organization. Bookings implementation starts next."
    />
  );
}
